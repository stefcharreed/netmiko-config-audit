# Copy to secrets.env and fill in. secrets.env is gitignored — never commit it.
# These values are read at runtime and merged with config/config.yaml.

# Default credentials, used for any device that doesn't override them in config.yaml
NET_USERNAME=admin
NET_PASSWORD=changeme

# Enable/secret password (only if your devices require it)
NET_SECRET=changeme
