settings:
  backup_dir:   ../config-backups
  baseline_dir: ../config-backups/baselines
  report_path:  ../config-backups/reports

devices:
  - name: ISR1
    host: 192.0.2.10
    device_type: cisco_ios
  - name: CAT1
    host: 192.0.2.11
    device_type: cisco_ios
  # credentials are omitted here on purpose — pulled from secrets.env at runtime
